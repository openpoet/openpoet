# OpenPoet Custom Instructions

## skill-creation-guide

# Skill Creation Guide

A skill in DevManager is a **markdown instruction template** stored in the database and synced to projects as `.claude/skills/<name>/SKILL.md` for Claude Code to follow during sessions.

This guide teaches you how to write effective skills that Claude Code will understand and follow correctly.

---

## CRITICAL RULE

- **NEVER** create skills without valid YAML frontmatter (name, description, user-invocable)
- **ALWAYS** test skills in a single project before syncing to all projects
- **NEVER** include YAML frontmatter in the `content` field when creating/updating via API (it's auto-generated)
- **ALWAYS** use lowercase-with-hyphens for skill names (e.g. `python-best-practices`, not `Python Best Practices`)
- **ALWAYS** include bilingual keywords (PT + EN) in descriptions and trigger examples for user-invocable skills

---

## Skill Anatomy

### YAML Frontmatter (auto-generated by DevManager)

```yaml
---
name: my-skill-name
description: "What this skill does and when to use it"
user-invocable: true  # or false
allowed-tools:        # optional, only for automation skills
  - Bash
  - Edit
  - Write
---
```

**Fields:**
- `name`: lowercase-with-hyphens, max 64 chars (auto-filled from DB)
- `description`: Clear explanation of when Claude Code should use this skill (auto-filled from DB)
- `user-invocable`:
  - `true` = automation skill (user triggers with keywords like "deploy", "test", "commit")
  - `false` = background knowledge (Claude Code reads but doesn't execute as a task)
- `allowed-tools`: (optional) restrict which tools Claude Code can use during execution

### Content Structure

```markdown
# Skill Title

Brief intro explaining what this skill does.

---

## CRITICAL RULE (if applicable)

- **NEVER** do X
- **ALWAYS** do Y
- Use this section for safety rules, production warnings, etc.

---

## Instructions

1. Step-by-step instructions
2. Use numbered lists for sequences
3. Use bullet points for guidelines

## Examples

\`\`\`bash
# Show concrete examples
make build
./devmanager &
\`\`\`

## Success Criteria (for automation skills)

- Output shows "✓ Build successful"
- Service responds on expected port
- No errors in logs

## Common Mistakes to Avoid

- Don't do X because Y
- Always check Z before running
```

---

## Skill Patterns

### Pattern 1: Automation Task (user-invocable: true)

**Use when:** User wants to trigger a specific workflow (deploy, test, commit, etc.)

**Example: Deploy Skill**

```yaml
---
name: deploy
description: "Deploy the application to production (.run directory, port 8081). Use when user says: deploy, fazer deploy, atualizar o serviço, subir o serviço, restart da produção, update production."
user-invocable: true
allowed-tools:
  - Bash
  - Read
---

# Deploy to Production

Deploy the DevManager application to the .run directory on port 8081.

---

## CRITICAL RULE

- **NEVER** deploy without running tests first
- **ALWAYS** create a git tag before deploying
- **NEVER** kill the production process (port 8081) unless explicitly deploying

---

## Instructions

1. Run tests: `make test`
2. Build binary: `make build`
3. Stop old production process: `pkill -f "devmanager -port 8081"`
4. Copy binary to .run: `cp build/devmanager .run/`
5. Start production: `cd .run && nohup ./devmanager -port 8081 &`
6. Verify service is up: `curl http://localhost:8081/health`

## Success Criteria

- Tests pass before deploy
- Binary copied to .run/
- Service responds on port 8081
- No errors in nohup.out

## Common Mistakes to Avoid

- Don't deploy without testing
- Don't forget to update .run/ directory
- Don't use port 8080 for production
```

---

### Pattern 2: Background Knowledge (user-invocable: false)

**Use when:** You want Claude Code to always follow certain guidelines/rules without explicit user trigger.

**Example: Python Best Practices**

```yaml
---
name: python-best-practices
description: "General Python coding standards. Claude Code should follow these automatically when writing Python code."
user-invocable: false
---

# Python Best Practices

Follow these guidelines when writing or editing Python code in this project.

---

## Code Style

- Use type hints in all function signatures
- Prefer `pathlib.Path` over `os.path`
- Use f-strings instead of `.format()` or `%`
- Max line length: 100 characters
- Use dataclasses for simple data containers

## Error Handling

- Catch specific exceptions, never bare `except:`
- Use `logging` module, not `print()` for debugging
- Validate inputs at function boundaries

## Testing

- Write unit tests for all public functions
- Use pytest fixtures for common setup
- Aim for >80% code coverage

## Common Mistakes to Avoid

- Don't use mutable default arguments
- Don't ignore type checker warnings
- Don't commit code with `print()` statements
```

---

### Pattern 3: Git Workflow (user-invocable: true)

**Example: Smart Commit**

```yaml
---
name: commit
description: "Create a git commit with a well-crafted message. Use when user says: commit, fazer commit, salvar alterações, commit changes, save changes."
user-invocable: true
allowed-tools:
  - Bash
  - Read
---

# Git Commit with Smart Message

Create a commit with a conventional commit message based on staged changes.

---

## Instructions

1. Check git status: `git status`
2. If no staged changes, ask user what to stage
3. Review diff: `git diff --staged`
4. Generate commit message following Conventional Commits format:
   - `feat:` for new features
   - `fix:` for bug fixes
   - `docs:` for documentation
   - `refactor:` for code refactoring
   - `test:` for tests
   - `chore:` for build/config changes
5. Create commit: `git commit -m "type: description"`

## Success Criteria

- Commit message follows Conventional Commits format
- Message is concise and descriptive
- User approves message before committing

## Common Mistakes to Avoid

- Don't commit without reviewing staged changes
- Don't use vague messages like "fix stuff"
- Don't auto-commit without user confirmation
```

---

## Bilingual Support (PT + EN)

For user-invocable skills, include both Portuguese and English keywords in the description so users can trigger the skill in either language.

**Example:**
```yaml
description: "Run tests using Playwright. Use when user says: test, testar, rodar testes, run tests, check the UI, verificar interface."
```

---

## Success Criteria for a Well-Written Skill

A skill is well-written when:

- ✅ It loads without errors in Claude Code session
- ✅ Description clearly indicates when to use it (with PT+EN keywords for user-invocable skills)
- ✅ Critical rules are highlighted and followed during execution
- ✅ Examples are accurate and tested in the actual project
- ✅ Success criteria are measurable and clear
- ✅ Common mistakes are documented and avoided
- ✅ `allowed-tools` restricts Claude Code to safe tools (for automation skills)

---

## Common Mistakes to Avoid

1. **Including YAML frontmatter in API calls**
   - ❌ Don't include `---\nname: foo\n---` in the `content` field
   - ✅ Use separate `name`, `category` parameters in API calls

2. **Using spaces in skill names**
   - ❌ `"Python Best Practices"`
   - ✅ `"python-best-practices"`

3. **Vague descriptions**
   - ❌ `"Useful commands"`
   - ✅ `"Deploy application to production. Use when user says: deploy, fazer deploy"`

4. **Missing critical rules**
   - ❌ No warnings about production/destructive operations
   - ✅ Use `## CRITICAL RULE` section with **NEVER**/**ALWAYS** statements

5. **Monolingual keywords**
   - ❌ Description only in English: `"Run tests"`
   - ✅ Bilingual: `"Run tests / Rodar testes. Use when user says: test, testar, run tests"`

6. **Over-permissive tools**
   - ❌ No `allowed-tools` restriction on automation skills
   - ✅ Restrict to necessary tools only: `["Bash", "Read"]`

7. **No success criteria**
   - ❌ Skill doesn't explain how to verify success
   - ✅ Clear, measurable success criteria listed

---

## Testing Your Skill

1. Create skill via DevManager API or UI
2. Sync to a **single test project** first
3. Start Claude Code session in that project
4. Trigger the skill (if user-invocable) or observe behavior (if background knowledge)
5. Verify Claude Code follows instructions correctly
6. Check for errors in skill parsing or execution
7. Once validated, sync to all projects

---

## Related Documentation

- Conventional Commits: https://www.conventionalcommits.org/
- Claude Code Skill Documentation: https://docs.anthropic.com/claude/docs/skills-overview

## commit

---
name: commit
description: Create a git commit with a well-crafted message. Use when the user asks to commit changes.
allowed-tools: Bash(git *)
---

# Git Commit Skill

## Rules

1. **Language**: All commit messages MUST be written in English, regardless of the language the user communicates in.

2. **No AI attribution**: NEVER include any reference to Claude, AI, or any automated tool as author, co-author, or in any form. Specifically:
   - Do NOT add `Co-Authored-By` lines mentioning Claude, Anthropic, AI, or any bot
   - Do NOT add `Signed-off-by` lines mentioning Claude or AI
   - Do NOT mention Claude, AI assistance, or automated generation anywhere in the commit message
   - The commit must appear as if it was entirely written by the human developer

3. **Message format**: Use the conventional commits style:
   - First line: short summary in imperative mood, max 72 characters (e.g., `fix: resolve login timeout on slow connections`)
   - Blank line after the subject
   - Optional body: explain the "why" behind the change, not the "what" (the diff already shows that)
   - Wrap body lines at 72 characters

4. **Commit type prefixes** (use when appropriate):
   - `feat:` - new feature
   - `fix:` - bug fix
   - `refactor:` - code restructuring without behavior change
   - `docs:` - documentation only
   - `style:` - formatting, whitespace, etc.
   - `test:` - adding or updating tests
   - `chore:` - maintenance tasks, dependencies, configs
   - `perf:` - performance improvements

## Process

1. Run `git status` and `git diff --staged` to understand staged changes. If nothing is staged, run `git diff` to see unstaged changes.
2. Run `git log --oneline -5` to see recent commit style for consistency.
3. Analyze the changes and draft an appropriate commit message.
4. Stage relevant files individually (avoid `git add -A` or `git add .`).
5. Create the commit using a HEREDOC for the message.
6. Run `git status` after committing to verify success.

$ARGUMENTS

## deploy

---
name: deploy
description: Deploy the OpenPoet application to the .run directory on port 8081. Use when the user asks to deploy, fazer deploy, atualizar o serviço, subir o serviço, restart da produção, or update the running production instance.
user-invocable: false
allowed-tools: Bash(./.scripts/deploy.sh *), Bash(make deploy*), Bash(sleep *)
---

# Deploy Skill

Deploy the OpenPoet service to the `.run/` directory on port 8081 using the deploy script.

## REGRA CRÍTICA — Usar SEMPRE o script `.scripts/deploy.sh`

- **NUNCA** executar os passos de deploy manualmente (kill, build, cp, nohup).
- **SEMPRE** usar o script `.scripts/deploy.sh` que já encapsula toda a lógica de deploy.
- O script se daemoniza automaticamente — sobrevive mesmo se o Claude Code ou o terminal cair.

## Steps

### 1. Executar o deploy

```bash
./.scripts/deploy.sh
```

O script faz tudo automaticamente:

- Mata apenas o processo na porta 8081 (nunca o 8080)
- Verifica que o dev server continua vivo
- Roda `make build`
- Copia o binário para `.run/`
- Inicia o serviço na porta 8081
- Verifica que ambas as portas estão saudáveis

O comando retorna imediatamente — o deploy roda em background.

### 2. Aguardar e verificar o resultado

Esperar alguns segundos para o deploy completar e checar o status:

```bash
sleep 8 && ./.scripts/deploy.sh --status
```

- Se o status for `SUCCESS`, reportar sucesso ao usuário.
- Se o status for `FAILED` ou `RUNNING`, checar o log com `./.scripts/deploy.sh --log` e reportar o erro.

### 3. Em caso de falha — consultar o log

```bash
./.scripts/deploy.sh --log
```

- Mostrar as linhas relevantes do log ao usuário para diagnóstico.

## testing

---
name: testing
description: Teste — Skill de Testes com Playwright
user-invocable: false
---

# Teste — Skill de Testes com Playwright

Usa o Playwright (via MCP) para testar a aplicação OpenPoet no navegador.

## !! REGRA CRÍTICA — NUNCA TESTAR EM PRODUÇÃO !!

- **NUNCA** usar a porta **8081** para testes. A porta 8081 é PRODUÇÃO e está em uso ativo.
- **NUNCA** navegar para `http://localhost:8081` durante testes.
- **NUNCA** matar ou interferir com o processo na porta 8081.
- Testes devem ser feitos **SEMPRE** na porta **8080** (desenvolvimento).
- Se o usuário passar `8081` como argumento, **RECUSAR** e explicar que é produção.

## Iniciar o servidor de desenvolvimento

Antes de testar, garantir que o OpenPoet está rodando na porta 8080. Para isso:

1. **Compilar** a aplicação:
   ```bash
   make build
   ```

2. **Iniciar** o servidor de desenvolvimento com bind em `0.0.0.0`:
   ```bash
   ./build/openpoet -port 8080 -bind 0.0.0.0
   ```

   > **IMPORTANTE:** O host padrão agora é `localhost`. Para que o Playwright consiga acessar, é necessário usar `-bind 0.0.0.0`.

3. Se o servidor já estiver rodando na porta 8080, não é necessário reiniciar — mas verificar que foi iniciado com `-bind 0.0.0.0`.

## URL padrão

- **SEMPRE usar:** `http://localhost:8080` (desenvolvimento)
- Se o usuário fornecer uma URL como argumento (`$ARGUMENTS`), usar essa URL — **exceto se for porta 8081**.

## Como testar

### 1. Navegar para a aplicação

```
browser_navigate → URL da aplicação
```

### 2. Capturar o estado da página

Sempre começar com um **snapshot** para entender a estrutura da página:

```
browser_snapshot → captura a árvore de acessibilidade
```

O snapshot é preferível ao screenshot para interações, pois retorna os `ref` dos elementos.

### 3. Interagir com elementos

Usar os `ref` retornados pelo snapshot para clicar, digitar, etc:

- `browser_click` — clicar em botões, links, tabs
- `browser_type` — digitar em campos de texto
- `browser_press_key` — pressionar teclas (Enter, Escape, Tab, etc)
- `browser_fill_form` — preencher múltiplos campos de formulário
- `browser_select_option` — selecionar opções em dropdowns

### 4. Verificar resultados

- `browser_snapshot` — verificar mudanças no DOM após interações
- `browser_take_screenshot` — capturar imagem visual da página
- `browser_console_messages` — verificar erros no console (level: "error")
- `browser_network_requests` — verificar chamadas de rede e respostas
- `browser_wait_for` — esperar texto aparecer/desaparecer

### 5. Testar responsividade mobile

Para testar a versão mobile:

```
browser_resize → width: 375, height: 812 (iPhone)
```

Depois fazer snapshot e verificar o layout mobile.

## Fluxo padrão de teste

1. **Navegar** para a URL
2. **Snapshot** para entender a página
3. **Verificar console** em busca de erros JavaScript
4. **Interagir** com elementos principais (tabs, botões, inputs)
5. **Screenshot** para documentar o estado visual
6. **Reportar** ao usuário o que foi encontrado

## Regras

- Sempre reportar ao usuário o que está sendo testado e os resultados
- Se encontrar erros no console, listar todos
- Se encontrar problemas visuais, tirar screenshot e descrever
- Ao final, fechar o browser com `browser_close` (a menos que o usuário queira continuar testando)
- Se o browser não estiver instalado e der erro, usar `browser_install` primeiro
